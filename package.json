{
    "name": "nein09-dot-com",
    "version": "1.1.1",
    "source": [
        "index.html",
        "sass/style.scss"
    ],
    "targets": {
        "default": {
            "distDir": "dist"
        }
    },
    "scripts": {
        "build": "parcel build",
        "watch": "parcel"
    },
    "devDependencies": {
        "@parcel/transformer-sass": "^2.16.1",
        "parcel": "^2.16.1"
    },
    "dependencies": {
        "npm": "^11.6.4"
    }
}
