{
    "name": "nein09-dot-com",
    "version": "1.1.2",
    "source": [
        "index.html",
        "sass/style.scss"
    ],
    "targets": {
        "default": {
            "distDir": "dist"
        }
    },
    "scripts": {
        "build": "parcel build",
        "watch": "parcel"
    },
    "devDependencies": {
        "@parcel/transformer-sass": "^2.16.4",
        "parcel": "^2.16.4"
    },
    "dependencies": {
        "npm": "^11.6.4"
    }
}
